# Telegram Бот для Распознавания Казахской Речи 🎤

Этот проект реализует Telegram бот, который распознаёт казахскую речь из голосовых сообщений и преобразует её в текст.

## Возможности

- 🎤 Распознавание казахской речи из голосовых сообщений
- 📝 Преобразование голоса в текст
- ⚡ Быстрая обработка благодаря GPU поддержке
- 🔄 Поддержка нескольких голосовых сообщений
- 📊 Отображение уверенности распознавания

## Требования

- Python 3.8+
- CUDA 11.0+ (рекомендуется для GPU ускорения)
- Telegram Bot API token

## Установка

### 1. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Если у тебя нет GPU или хочешь использовать CPU:
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
```

### 3. Конфигурация

Создай файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируй `.env` и добавь свой Telegram Bot Token:

```env
TELEGRAM_BOT_TOKEN=твой_токен_бота
MODEL_PATH=../whisper-finetuned-kk
```

### 4. Получение Telegram Bot Token

1. Откройся с [@BotFather](https://t.me/botfather) в Telegram
2. Используй команду `/newbot`
3. Следуй инструкциям для создания бота
4. Скопируй полученный token в `.env` файл

## Использование

### Запуск бота

```bash
python bot.py
```

### Команды бота

- `/start` - Начальная информация о боте
- `/help` - Справка по использованию
- `/about` - Информация о боте

### Отправка голосовых сообщений

1. Откройся с ботом в Telegram
2. Нажми на микрофон
3. Запиши сообщение на казахском языке
4. Отправь голосовое сообщение
5. Получи результат распознавания

## Структура проекта

```
tg_bot_srs/
├── bot.py                 # Основной файл Telegram бота
├── speech_recognizer.py   # Модуль для распознавания речи
├── config.py             # Конфигурация проекта
├── requirements.txt      # Зависимости проекта
├── .env.example         # Пример конфигурации
├── .env                 # Конфигурация (не комитить!)
├── temp_audio/          # Временная папка для аудиофайлов
└── README.md            # Этот файл
```

## Технология

### Используемые библиотеки

- **python-telegram-bot** - API для работы с Telegram
- **transformers** - Загрузка и использование моделей HuggingFace
- **torch/torchaudio** - Обработка аудио и выполнение моделей
- **librosa** - Обработка аудиосигналов
- **python-dotenv** - Загрузка переменных окружения

### Модель

Проект использует **Whisper** модель, файн-тюнированную для казахского языка.
Модель расположена в папке `../whisper-finetuned-kk`

## Возможные ошибки и решения

### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"
- Проверь что у тебя создан файл `.env`
- Убедись что в нём указан TELEGRAM_BOT_TOKEN

### Ошибка: "CUDA out of memory"
- Используй CPU вместо GPU: отредактируй `config.py` и установи `DEVICE = "cpu"`
- Или сократи размер батча в `speech_recognizer.py`

### Ошибка: "Model not found"
- Проверь что модель существует в пути указанном в `MODEL_PATH`
- Убедись что путь правильный

### Бот не отвечает
- Проверь что токен правильный
- Убедись что у бота есть интернет соединение
- Проверь логи для получения более подробной информации

## Развитие проекта

Возможные улучшения:

- [ ] Добавить поддержку других языков
- [ ] Реализовать кэширование часто используемых фраз
- [ ] Добавить обработку ошибок распознавания
- [ ] Реализовать пользовательские настройки
- [ ] Добавить статистику использования
- [ ] Оптимизировать использование памяти
- [ ] Добавить веб-интерфейс для управления

## Лицензия

Этот проект создан в рамках лабораторной работы по компьютерному зрению и распознаванию речи.

## Контакты

Если у тебя есть вопросы или проблемы, свяжись с разработчиком.

---

**Версия:** 1.0  
**Последнее обновление:** Ноябрь 2025
